name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Fake deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deployment placeholder
        run: echo "Aquí irá el proceso de deploy"